{
  "name": "indicana-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "INDICANA frontend packaged for web and Android via Capacitor.",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "serve": "npx -y serve .",
    "android:init": "npm run build && npx cap add android",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npx cap open android",
    "apk": "npm run android:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  }
}
